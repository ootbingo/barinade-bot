lily:
  commandline:
    enabled: false

spring:
  datasource:
    url: jdbc:tc:postgresql:15-alpine:///barinade
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
  main:
    banner-mode: "off"
  jpa:
    show-sql: false

logging:
  file:
    name:
  level:
    root: OFF
    org:
      springframework: OFF
    ootbingo: DEBUG
